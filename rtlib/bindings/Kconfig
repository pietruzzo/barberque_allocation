
config BBQUE_BINDINGS_PYTHON
  depends on TARGET_LINUX_NATIVE
  bool "Python wrapper"
  select EXTERNAL_PYBIND11
  default n
  ---help---
  Build the module to provide the Python wrapper of the RTLib

